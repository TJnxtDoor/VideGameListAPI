# VideGameListAPI

# TaskTracker Capstone

## Instructions to Run the Project

1. **Start the Web API:**
   Open a terminal window and run:
   in bash 
   cd TaskTracker.Api
   dotnet run --urls "http://localhost:5000"